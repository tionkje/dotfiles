# Plan: close functional gaps in the hyprland.lua conversion

Gaps found comparing `hypr/.config/hypr/hyprland.lua.draft` against
`hypr-conf/.config/hypr/hyprland.conf` (Hyprland 0.55.4, lua engine).
The port is otherwise faithful: all binds, 9/9 workspace rules, window/layer
rules, animations, groupbar/input/device settings match.

## 0. Safety gate for every activation (live daily-driver setup)

`Hyprland --verify-config --config FILE` works on lua configs and is strict:
unknown rule effect keys and bad monitor mode strings are hard errors, not
warnings. The current draft already passes (`config ok`, verified on 0.55.4).
Caveat: verify-config picks the parser by file extension — run it on a file
named `.lua`; the in-place `.draft` is parsed as conf syntax and fails.

- [ ] Never restart into the lua engine without a passing
      `Hyprland --verify-config --config ~/.config/hypr/hyprland.lua`
      run *after* stowing (so the monitors.lua include resolves too)
- [ ] Also run it on the package file *before* stowing — Hyprland autoreloads
      on config changes by default, and "engine selection happens at startup,
      not on reload" is unverified; if a live reload can pick up the .lua
      mid-swap, the gate makes the worst case a working config
- [ ] Rollback path if the session won't come up: TTY (Ctrl+Alt+F3),
      `hypr-engine conf`, log back in. `Hyprland --safe-mode` also exists.

## 1. Restore systemd session target startup

`hyprland.conf:76` runs:

```
dbus-update-activation-environment --systemd WAYLAND_DISPLAY HYPRLAND_INSTANCE_SIGNATURE XDG_CURRENT_DESKTOP && systemctl --user start hyprland-session.target
```

The draft omits it and launches `hypridle` directly instead (draft line 62).
This breaks every `WantedBy=graphical-session.target` user service — verified:
hypridle.service is enabled with `WantedBy=graphical-session.target`, so the
target line is load-bearing and the direct exec would double-start hypridle.

- [ ] Add the dbus/systemctl line to the `hyprland.start` handler — the `&&`
      chain is fine, `hl.exec_cmd` has shell semantics (official example uses
      `"waybar & hyprpaper & firefox"`)
- [ ] Remove the direct `hl.exec_cmd("hypridle")`
- [ ] Keep `setsid` on monitor-handler.sh — the conf has
      `exec-once = setsid ~/.config/hypr/monitor-handler.sh`, the draft
      (line 64) dropped it; the script is a long-running event loop

## 2. Restore Chrome/Brave notification popup rules

`hyprland.conf:104-106`: float + pin + no_initial_focus on
`match:class ^([Gg]oogle-chrome|[Cc]hromium|[Bb]rave).*$` +
`match:initial_title .*[Nn]otification.*`. Missing from the draft entirely.

- [ ] Port as `hl.window_rule` calls with `no_initial_focus = true` —
      RESOLVED: `--verify-config` accepts `no_initial_focus` (same key as the
      conf) and rejects unknown keys, so the stub-example `no_focus` guess is
      not needed

## 3. Restore per-machine monitor config

The conf `source`s `~/.config/hypr/monitors.conf` — a local, non-symlinked,
machine-specific file. The draft hardcodes the monitor table, so a second
machine can no longer override.

- [ ] Replace the hardcoded table with a local include. NOT
      `pcall(dofile, …)` — pcall swallows a syntax error in monitors.lua
      silently, and `--verify-config` would still print `config ok` with zero
      monitors configured. Existence check instead, so missing file = default
      and broken file = loud verify-config failure:

      ```lua
      local monitors_lua = os.getenv("HOME") .. "/.config/hypr/monitors.lua"
      local f = io.open(monitors_lua)
      if f then
          f:close()
          dofile(monitors_lua)
      else
          hl.monitor({ output = "", mode = "preferred", position = "auto-center-up", scale = "auto" })
      end
      ```
- [ ] Create `~/.config/hypr/monitors.lua` on this machine from the current
      table (values already match the live monitors.conf)

## 4. Engine-switch gap in hypr-engine

The old script created the new `hyprland.*` symlink *before* removing the
other one, with a comment that Hyprland regenerates a default stub when its
config path goes missing. The stow-based rewrite does `stow -D` then `stow`,
leaving a window with no config file; if the stub gets written in that
window, the restow conflicts and fails.

The regeneration mechanism is real: the 0.55.4 binary carries the
autogenerated-config template, including a lua variant
(`hl.config({ autogenerated = true })`). Whether it triggers live on deletion
or only at startup is unverified.

BLOCKED ON hypr-common (cleanup plan §1): the stow-new-first fix assumed "the
packages share no filenames" — false once hypr-lua mirrors hypr-conf's layout
(4 shared files). Stow refuses to stow over another package's symlinks, so
the reorder would conflict and abort. With hypr-common the engine packages
are disjoint (one config file each) and the reorder is safe in both
directions — the stub-regeneration question becomes moot, and today's broken
rollback (`stow hypr-conf` conflicting on the 4 shared files) is fixed too.

- [ ] After hypr-common exists: stow the new engine package first,
      `stow -D` the old one after (both directions in hypr-engine)

## 5. Verify the draft's open TODOs after activation

Parse-level answers obtained via `Hyprland --verify-config` probes on 0.55.4
(it validates rule keys and mode-string format strictly — a garbage rule key
and a bogus mode both hard-error — so none of these can break parsing or
boot):

- [x] `fullscreen_state = "0 2"` — accepted
- [x] `group = "set always"` / `group = "deny"` — accepted
- [x] `hl.dsp.window.fullscreen(1)` — accepted
- [x] workspace rule `monitor = "DP-3, HDMI-A-1"` — accepted
- [x] `2560x1440@59.95Hz` mode string — accepted (validated, not passed
      through: `mode = "totally-bogus"` errors)

Still needs runtime verification with `hyprctl` after activation (semantics,
not syntax):

- [x] `fullscreen(1)` does NOT maximize — arg is ignored, always real fullscreen
      (internal 2 + client 2). Fixed with
      `fullscreen_state({ internal = 1, client = 0, action = "toggle" })`,
      verified via `hyprctl eval` + `hyprctl activewindow -j`
- [ ] `fullscreen_state = "0 2"` gives fake fullscreen on brave-youtube
- [ ] `group = "set always"` groups spotify/read windows
- [ ] comma-separated monitor actually falls back — low stakes,
      monitor-handler.sh reassigns at runtime anyway
