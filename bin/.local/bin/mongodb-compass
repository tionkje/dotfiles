#!/usr/bin/env bash
#export NIXPKGS_ALLOW_UNFREE=1
#exec nix run --impure 'github:nixos/nixpkgs/25.05#mongodb-compass'

export NIXPKGS_ALLOW_UNFREE=1 
export ELECTRON_OZONE_PLATFORM_HINT=auto 
nix run --impure github:nix-community/nixGL -- nix run --impure 'github:nixos/nixpkgs/25.05#mongodb-compass' -- --ignore-additional-command-line-flags --password-store=gnome-libsecret
